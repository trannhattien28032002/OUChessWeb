// const history = require("../models/history");

// const historyRepository = {
//     addHistory: async (params) => {
//         try {
//             const newHistory = await history.save();
//             return newHistory;
//         } catch (error) {
//             console.log(error.message);
//             return null;
//         }
//     },
// };

// module.exports = historyReposity;
